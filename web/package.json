{
  "name": "wb26wc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WB26WC — framework-free World Cup 2026 draft-pool tracker (static, zero runtime deps)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1"
  }
}
