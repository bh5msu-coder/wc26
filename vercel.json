{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && if [ -n \"$DATABASE_URL\" ]; then export DIRECT_URL=\"${DIRECT_URL:-$DATABASE_URL}\"; prisma db push --accept-data-loss && (tsx prisma/seed.ts || echo 'seed step skipped'); else echo 'no DATABASE_URL set — skipping db push/seed'; fi && next build",
  "installCommand": "npm install"
}
